# Starknet Network Configuration
# Maps to sncast profile: default, sepolia, or mainnet
# The profile should be configured in snfoundry.toml with account and url
STARKNET_NETWORK=sepolia

# RPC Endpoint (optional - overrides the url in snfoundry.toml profile)
# Only set this if you want to override the profile's configured url
# STARKNET_RPC=https://starknet-sepolia.infura.io/v3/YOUR_API_KEY

# Owner Contract Address
# The address of the tournament contract (e.g., Budokan) that will own the validators
OWNER_ADDRESS=0x...

# Registration Only Mode (optional, defaults to false)
# Set to "true" to only allow registration validation (not entry validation)
# Note: Some validators (like ERC20BalanceValidator) have this hardcoded
REGISTRATION_ONLY=false

# Skip deployment confirmation prompt (optional, useful for CI/CD)
SKIP_CONFIRMATION=false
